import '@lit-labs/ssr-client/lit-element-hydrate-support.js';
import './registerComponents';

// import { treaty } from "@elysiajs/eden";

// const api = treaty<App>("localhost:3000");

// const {pathname} = location;
// if(pathname.split('/').length > 2) {
// const roomId = pathname.split('/').at(-1)!;

// console.log(roomId)

// const chat = api?.chat.subscribe({query: {roomId}});

// chat.subscribe((message) => {
//   console.log("got", message);
// });

// chat.on("message",(e) => {
//   console.log('on message', e)
//   messages.insertAdjacentHTML('beforeend',`<li>${e.data.message}</li>`)
// });

// chat.on("open", () => {
//   chat.send({
//     user: 'me',
//       user: 'me',
//       message: target.value,
//       roomId
//     });
//     target.value = '';
//   }
// })
// }message: 'me entered the room',
//     roomId
//   });
// });

// messageInput.addEventListener('keydown', ({key, target}) => {
//   if(key === 'Enter') {
//     chat.send({
//     


console.log('i do add this so ?')